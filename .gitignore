# Log files the dev server writes (machine noise, not code)
server.log
*.log

# Local tool folders that vary per computer
.freebuff/
node_modules/
.idea/

# Dedicated demo browser profiles (Chrome/Edge cache, created by start-demo.bat)
demo-profile/

# OS junk (Windows/Mac files that mean nothing to git)
Thumbs.db
.DS_Store
